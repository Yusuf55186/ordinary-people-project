/**
 * Shared desk-shot layout. Keep scene-specific acting and props out of here.
 * These values preserve the established Episode 2 placement exactly.
 */
export const yusufDeskIntegration = {
  typing: { x: 920, y: 380, width: 500, scale: 0.5 },
  modular: { x: 858, y: 343, width: 500, scale: 0.75 },
} as const;
